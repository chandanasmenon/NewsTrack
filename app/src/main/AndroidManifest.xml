<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".NewsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/newsapp_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/newsapp_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsTrack"
        tools:targetApi="31">
        <activity
            android:name=".ui.homescreen.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.topheadlinesources.TopHeadlineSourcesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.pagingtopheadlinesources.TopHeadlinesPagingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.offlinetopheadlines.OfflineTopHeadlinesActivity"
            android:exported="false" />
    </application>

</manifest>